import { UsuarioCadastroResponse } from './usuario-cadastro-response';

describe('UsuarioCadastroResponse', () => {
  it('should create an instance', () => {
    expect(new UsuarioCadastroResponse()).toBeTruthy();
  });
});
