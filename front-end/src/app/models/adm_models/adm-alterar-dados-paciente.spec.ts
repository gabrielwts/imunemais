import { AdmAlterarDadosPaciente } from './adm-alterar-dados-paciente';

describe('AdmAlterarDadosPaciente', () => {
  it('should create an instance', () => {
    expect(new AdmAlterarDadosPaciente()).toBeTruthy();
  });
});
