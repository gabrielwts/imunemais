import { RecuperarEmailSenhaCadastro } from './recuperar-email-senha-cadastro';

describe('RecuperarEmailSenhaCadastro', () => {
  it('should create an instance', () => {
    expect(new RecuperarEmailSenhaCadastro()).toBeTruthy();
  });
});
