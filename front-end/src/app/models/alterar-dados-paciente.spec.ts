import { AlterarDadosPaciente } from './alterar-dados-paciente';

describe('AlterarDadosPaciente', () => {
  it('should create an instance', () => {
    expect(new AlterarDadosPaciente()).toBeTruthy();
  });
});
